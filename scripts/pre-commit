#!/bin/sh
# Pre-commit hook to lint backlog and changelog

# Check if steam-pick binary exists, if not build it
if [ ! -f bin/steam-pick ]; then
    echo "Building steam-pick..."
    go build -o bin/steam-pick cmd/steam-pick/main.go
fi

# Run lint
bin/steam-pick backlog lint --fail-on error
